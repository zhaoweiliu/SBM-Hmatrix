%!PS-Adobe-3.0 EPSF-3.0
%Title: 
%Creator: TPSPrinter
%Orientation: Portrait
%%BoundingBox: 15 15 525 275
%Pages: 0
%BeginSetup
%EndSetup
%Magnification: 1.0000
%EndComments
/PSPrinterDict 200 dict def
PSPrinterDict begin
/BD   { bind def } bind def
/X    { exch } bind def

/NP   { newpath } bind def
/M    { moveto } bind def
/L    { lineto } bind def
/RL   { rlineto } bind def
/DA   { NP arc closepath } bind def
/CP   { closepath } bind def
/S    { stroke } bind def
/F    { fill } bind def
/R    { roll } bind def

/DL   { 4 2 roll
        newpath
        moveto lineto
        closepath
        stroke } bind def
/DR   { newpath
        4 2 roll
        moveto exch dup 0 rlineto
        exch 0 exch rlineto
        -1 mul 0 rlineto
        closepath stroke } bind def
/FR   { newpath
        4 2 roll
        moveto exch dup 0 rlineto
        exch 0 exch rlineto
        -1 mul 0 rlineto
        closepath fill } bind def
/DT   { newpath
        moveto lineto lineto
        closepath stroke } bind def
/FT   { newpath
        moveto lineto lineto
        closepath fill } bind def
/DC   { newpath
        0 360 arc
        closepath stroke } bind def
/FC   { newpath
        0 360 arc
        closepath fill } bind def

/SW   { setlinewidth } bind def
/SD   { setdash } bind def
/SRGB { setrgbcolor } bind def
/SG   { setgray } bind def
/FF   { findfont } bind def
/SF   { setfont } bind def
/SCF  { scalefont } bind def
/TB   { 3 1 roll
        translate 1 -1 scale
      } bind def
/TL   { gsave
          TB newpath 0 0 moveto show
        grestore } bind def
/TR   { gsave
          TB
          dup stringwidth pop
          newpath neg 0 moveto show
        grestore } bind def
/TC   { gsave
          TB
          dup stringwidth pop 2 div
          newpath neg 0 moveto show
        grestore } bind def
/OTL  { gsave
          TB 0 0 moveto true charpath stroke
        grestore } bind def
/OTR  { gsave
          TB
          dup stringwidth pop
          neg 0 moveto true charpath stroke
        grestore } bind def
/OTC  { gsave
          TB
          dup stringwidth pop 2 div
          neg 0 moveto true charpath stroke
        grestore } bind def
/GS   { gsave } bind def
/GR   { grestore } bind def

end
%EndProlog
PSPrinterDict begin
/psprnsavedpage save def
GS
1 SW
0 SG
1 -1 scale
0 -250 translate
20 -20 translate
62.5 0 scale
/Helvetica FF 4 SCF SF
0.5 SW
0 nan (0) TC
1 nan 0.1 FC
2 nan 0.1 FC
3 nan 0.1 FC
4 nan 0.1 FC
5 nan 0.1 FC
6 nan 0.1 FC
7 nan 0.1 FC
7 nan (7) TC
GR
psprnsavedpage restore
end
showpage
%EOF
